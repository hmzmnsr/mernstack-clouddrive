export enum PageNames {
  HOME = "HOME",
  FILES = "FILES",
  FOLDERS = "FOLDERS",
  FAVORITES = "FAVORITES",
  SETTINGS = "SETTINGS",
}

export enum PageUrls {
  HOME = "/dashboard",
  FILES = "/files",
  FOLDERS = "/folders",
  FAVORITES = "/favorites",
  SETTINGS = "/settings",
}
 